# UPF 2.1 — Power intent for uart_axi_periph (single power domain).
# The peripheral (AXI-Lite register file + UART stack + TX/RX glue) is
# entirely single-clock, always-on — no domain crossings, no power switches.

create_power_domain PD_TOP -include_scope

create_supply_net  VDD -domain PD_TOP
create_supply_net  VSS -domain PD_TOP

connect_supply_net VDD -ports { VDD }
connect_supply_net VSS -ports { VSS }

create_supply_set  SS_TOP \
    -function {power VDD} \
    -function {ground VSS}

associate_supply_set SS_TOP -handle PD_TOP.primary
